FROM rasa/rasa:3.6.20-spacy-en

WORKDIR /app

# Copy project files
COPY . .

# Copy model as latest.tar.gz
COPY models/20251209-223435-ivory-ayu.tar.gz models/latest.tar.gz

EXPOSE 5005

CMD ["rasa", "run", "--model", "models/latest.tar.gz", "--enable-api", "--cors", "*"]
